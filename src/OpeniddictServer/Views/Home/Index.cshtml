@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    

    @if (User.Identity.IsAuthenticated)
    {
        <h1 class="display-4">Welcome @User.Identity.Name</h1>
    }
    else
    {
    <h1 class="display-4">Log In or Sign In</h1>
        <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Login">Click here for Login</a>
    }
</div>
